apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: dst-airlines
spec:
  selector:
    app: dashboard
  ports:
    - protocol: TCP
      port: 8050
      targetPort: 8050
  type: NodePort